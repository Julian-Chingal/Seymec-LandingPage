---
import Sections from "@/components/sections/sections.astro";
import { contactMethods } from "@/data/contacData";
---

<Sections title="Contactos">
    <div class="max-w-screen-xl mx-auto px-4 text-gray-600 md:px-8">
        <div class="max-w-lg mx-auto gap-12 justify-between lg:flex lg:max-w-none">
            <div class="max-w-lg space-y-3">
                <p class="text-gray-800 text-3xl font-semibold sm:text-4xl">
                    Déjanos saber cómo podemos ayudarte
                </p>
                <p>
                    Estamos aquí para ayudarte y responder cualquier pregunta que puedas tener. ¡Esperamos tener noticias tuyas! Completa el formulario o envíanos la información de contacto que aparece a continuación.
                </p>
                <div>
                    <ul class="mt-6 flex flex-col gap-x-10 gap-y-6 items-start">
                        {
                            contactMethods.map((item, idx) => (
                                <li class="flex items-center gap-x-3">
                                    <div class="flex-none text-secondary">
                                        <i class={`${item.icon} w-auto h5`}></i>
                                    </div>
                                    <p>{item.name}</p>
                                </li>
                            ))
                        }
                    </ul>
                </div>
            </div>
            <div class="flex-1 mt-12 sm:max-w-lg lg:max-w-md">
                <form
                    onsubmit="handleSubmit"
                    class="space-y-5"
                >
                    <div>
                        <label class="font-medium">
                            Nombre Completo
                        </label>
                        <input
                            type="text"
                            required
                            class="w-full mt-2 px-3 py-2 text-gray-500 bg-transparent outline-none border focus:border-primary shadow-sm rounded-lg"
                        />
                    </div>
                    <div>
                        <label class="font-medium">
                            Email
                        </label>
                        <input
                            type="email"
                            required
                            class="w-full mt-2 px-3 py-2 text-gray-500 bg-transparent outline-none border focus:border-primary shadow-sm rounded-lg"
                        />
                    </div>
                    <div>
                        <label class="font-medium">
                            Compañia
                        </label>
                        <input
                            type="text"
                            required
                            class="w-full mt-2 px-3 py-2 text-gray-500 bg-transparent outline-none border focus:border-primary shadow-sm rounded-lg"
                        />
                    </div>
                    <div>
                        <label class="font-medium">
                            Mensaje
                        </label>
                        <textarea required class="w-full mt-2 h-36 px-3 py-2 resize-none appearance-none bg-transparent outline-none border focus:border-primary shadow-sm rounded-lg"></textarea>
                    </div>
                    <button
                        class="w-full px-4 py-2 text-white font-medium bg-primary hover:bg-primary/75 active:bg-primary rounded-lg duration-150"
                    >
                        Enviar
                    </button>
                </form>
            </div>
        </div>
    </div>
</Sections>
---
import Banner from "@/components/landing/Banner.astro";
import Content from "@/components/landing/Content.astro";
import { services } from "@/data/servicesData";

const data = services.find(
  (service) => service.title === "Automatización y Control de Procesos"
);

const content = [
  {
    title: "Automatización de procesos",
    description:
      "Automatizacion de procesos electronicos e industriales, para mejorar la eficiencia y productividad de las empresas.",
  },
  {
    title: "Sistemas contra incendios",
    description:
      "Instalación y mantenimiento de sistemas contra incendios, para la protección de vidas y bienes.",
  },
];
---

<Banner title={data?.title} img={data?.img}>
  <Content title={data?.description || "Default Description"} data={content} />
</Banner>
